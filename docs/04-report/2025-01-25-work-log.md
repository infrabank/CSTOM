# 작업 이력 - 2025-01-25

## 개요
- **브랜치:** 001-cstom-mvp
- **작업자:** Claude Code Assistant
- **커밋 수:** 9개

---

## 1. PDF 보고서 출력 개선

**커밋:** `55037f0`

**목적:** 보고서 PDF 다운로드 시 두 장으로 출력되던 문제를 한 장으로 출력되도록 수정

**변경 파일:**
- `frontend/src/app/globals.css`

**상세 내용:**
- `@media print` 미디어 쿼리 추가
- A4 페이지 크기, 10mm 마진 설정
- 버튼, 네비게이션 링크 등 불필요한 요소 숨김
- 폰트 크기, 패딩, 간격 축소하여 한 페이지에 맞춤
- `page-break-inside: avoid`로 페이지 분리 방지

---

## 2. 보고서 한글화

**커밋:** `8594216`

**목적:** 보고서 생성 시 기본 내용이 영문으로 출력되던 것을 한글로 변경

**변경 파일:**
- `backend/reports/services.py`

**상세 내용:**

| 보고서 유형 | 변경 전 | 변경 후 |
|------------|--------|--------|
| 월간 보고서 | Monthly Report | 월간 보고서 |
| 장애 보고서 | Incident Report | 장애 보고서 |
| 감사 보고서 | Audit Summary | 감사 보고서 |

- 작업 유형 라벨 (maintenance, inspection 등) 한글 매핑
- 역할명 라벨 (operator, manager 등) 한글 매핑
- 상태값, 위험 플래그 등 한글화

---

## 3. 보고서 수정/삭제 기능

**커밋:** `2af0a48`, `5a58dca`

**목적:** 생성된 보고서의 수정 및 삭제 기능 추가

**변경 파일:**
- `backend/reports/views.py`
- `frontend/src/app/(admin)/reports/actions.ts`
- `frontend/src/app/(admin)/reports/delete-button.tsx` (신규)
- `frontend/src/app/(admin)/reports/[reportId]/page.tsx`
- `frontend/src/app/(admin)/reports/[reportId]/edit/page.tsx`

**상세 내용:**

**백엔드:**
- `ReportViewSet.get_permissions()`에 `update`, `partial_update` 액션 권한 추가

**프론트엔드:**
- `deleteReport` 서버 액션 추가
- `DeleteButton` 클라이언트 컴포넌트 생성 (확인 다이얼로그 포함)
- 보고서 상세 페이지에 수정/삭제 버튼 추가
- 수정 페이지 API 호출 시 인증 토큰 추가 (버그 수정)

---

## 4. 장비 반출입 폐기 기능

**커밋:** `8c324d6`, `7814ece`, `458b4d8`, `ca81b0f`

**목적:** 장비 목록에서 수정/삭제 기능 추가 (백엔드 정책에 따라 삭제 대신 폐기로 구현)

**변경 파일:**
- `frontend/src/app/(admin)/equipments/[equipmentId]/page.tsx`
- `frontend/src/lib/api.ts`
- `backend/equipments/serializers.py`

**상세 내용:**

**초기 구현:**
- 삭제 버튼 및 `handleDelete` 함수 추가

**정책 반영:**
- 백엔드에서 장비 삭제 금지 정책 확인
- 에러 메시지: "Equipment records cannot be deleted. Set status to 'retired' instead."
- 삭제 버튼 -> 폐기 버튼으로 변경
- `equipmentsApi.delete()` -> `equipmentsApi.update({ status: "retired" })`로 변경

**타입 수정:**
- `EquipmentCreateInput` 인터페이스에 `status` 필드 추가
- `EquipmentCreateSerializer`에 `status` 필드 추가

**UI 동작:**
- 이미 폐기된 장비(`retired`)나 반출중인 장비(`checked_out`)에서는 폐기 버튼 숨김

---

## 5. 모바일 헤더 레이아웃 개선

**커밋:** `d606151`

**목적:** 모바일 화면에서 헤더 레이아웃 재구성

**변경 파일:**
- `frontend/src/components/admin-header.tsx`

**상세 내용:**

**변경 전:**
- 로고(좌) / 로그아웃+햄버거메뉴(우)

**변경 후:**
- 햄버거 메뉴(좌) / 로고(중앙) / 로그아웃(우)

**구현 방식:**
- 데스크톱/모바일 헤더 분리 (`hidden md:flex` / `md:hidden flex`)
- 모바일 헤더 3단 구성 (`flex-1` 활용)
- 모바일 로고 크기 조정 (150x33 -> 120x27)

---

## 커밋 요약

| 커밋 해시 | 메시지 | 유형 |
|----------|--------|------|
| `d606151` | feat: reorganize mobile header layout with centered logo | 기능 |
| `ca81b0f` | fix: add status field to equipment update serializer | 수정 |
| `458b4d8` | fix: add status field to EquipmentCreateInput type | 수정 |
| `7814ece` | fix: change delete to retire for equipment management | 수정 |
| `8c324d6` | feat: add delete functionality to equipment detail page | 기능 |
| `5a58dca` | fix: add auth token to report edit page API calls | 수정 |
| `2af0a48` | feat: add edit and delete functionality to reports | 기능 |
| `8594216` | fix: localize report summaries to Korean | 수정 |
| `55037f0` | fix: print report as single page PDF | 수정 |

---

## 분류별 요약

| 구분 | 내용 |
|------|------|
| **신규 기능** | 보고서 수정/삭제, 장비 폐기, 모바일 헤더 개선 |
| **UI 개선** | PDF 출력 최적화, 모바일 헤더 레이아웃 |
| **한글화** | 보고서 생성 내용 (월간/장애/감사) |
| **버그 수정** | 인증 토큰 누락, 타입 정의 누락 |
